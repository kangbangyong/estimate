# 그리다 견적

GitHub Pages + Google Sheets(Apps Script) 로 돌아가는 견적 산출 도구.

- 화면: 이 저장소 `index.html` (GitHub Pages)
- 데이터: 구글 스프레드시트 「그리다 견적 DB」 — 단가DB · 견적이력 · 견적상세 · 설정
- API: 같은 시트의 Apps Script 웹앱 (`AppsScript_Code.gs`)

처음 열면 ⚙ 에서 Apps Script `/exec` 주소와 TOKEN 을 한 번 입력합니다. 브라우저에만 저장됩니다.
